# 進度

最後更新 2026-09-06

---

## 現在的狀態

**設計與 demo 原型完成，程式未開始。**

三站 demo 頁面已做完並發佈，是可以直接上台跑的互動原型（單檔 HTML，前端假資料，無後端）。痛點盤點、功能分層、差異化論述都已收斂成文件。還沒有寫任何真正的對帳引擎。

---

## 已完成

### 設計與論述

- **目標與範圍**：定位、白板五痛點、14 項缺口（四組）、機制設計、P0 五項／P1 四項／P2 八項 → `docs/01-solution.md`（2026-09-05 由原 00 與 01 合併）
- **差異化論述**：回應「OpenAI 加 MCP 也做得到」的挑戰 → `docs/02-差異化論述.md`
- **三站 demo 流程**：上傳與方向 → 目標樹草案 → 季度對帳

### 原型頁面（皆已發佈，網址見 CLAUDE.md）

| 站 | 狀態 | 互動元素 |
|---|---|---|
| 1 · 上傳與方向 | 完成 | 六個 role model 樣板可選，選了即時重算反差比對；三個約束問題；先放掉哪一項 |
| 2 · 目標樹草案 | 完成 | 路徑吸引力滑桿；分支手風琴（四要素／重測／效果假設）；三個追問可作答；接受並鎖定會改變全頁狀態 |
| 3 · 季度對帳 | 完成 | 空檔調度三軸即時重算；週檢查對／不對；排程草案只能刪，刪到低於最低週量會跳斷鏈警告 |

三頁時間軸已對齊：計畫 2026-06-28 起草 → Q3 對帳 2026-09-04。

---

## 決策紀錄

記錄「為什麼這樣做」，因為這些理由從畫面上看不出來。

### 2 分鐘影片走 Open Slide，無旁白（2026-09-06）

黑客松要交一支 2 分鐘產品影片。腳本定稿在 `pitch/影片腳本-120s.md`，14 頁、合計 120 秒。
`pitch/demo-腳本.md`（220 秒現場版）不動，兩份素材分開。

四個取捨：

**載體選 Open Slide，不是錄真實操作。** 120 秒沒有空間讓瀏覽器切分頁、等頁面載入。做成
1920×1080 的固定畫布投影片，每一秒都算得出來。

**只有站 1 用真畫面，站 2/站 3 用 slide 重述。** 站 1 已經整份換成 mist，站 2/站 3 還是舊的
petrol + Noto Serif TC。三站直接入鏡會像三個產品——那正是 `CLAUDE.md` 明講要避免的。等站
2/站 3 也搬到 mist 之後可以回頭改，但那不該擋著影片。

**無旁白，純字卡加背景音樂。** 代價是所有訊息都得印在畫面上，於是腳本裡每一頁都標了
「必讀字」預算，上限每秒 5 字。第 12、13、14 頁原本超標，砍了三個句尾、並把收尾頁從 8 秒
加到 10 秒（從結構圖與對帳表各借 1 秒）。被砍掉的半句留給現場 5 分鐘簡報。

**結構圖沿用白板那條管線，補上第四段「對帳」。** `個人設計系統/styles/mist/pages/architecture.html`
畫的 Uploader → Profile → Analyzer → Report → Recommender → Role Model → Plan Engine →
Milestone → Task → Schedule，就是 `docs/Untitled-2026-09-05-2220.excalidraw` 上那條管線的
英文版，而且對得起來——「Recommend 6 role model templates」就是站 1 的六個形狀 S-1…S-6。
它唯一缺的是對帳：整條是單向的「資料進 → 計畫出」，而本專案的主張是回饋迴圈。所以新增
④ 對帳（痕跡 → 歸戶 → 對帳引擎 → 診斷），並用一條 accent 粗線把診斷接回 Plan Engine。
那條線是整張圖唯一要讓評審看見的東西。

deck 只引 mist 的 token 與 components.css，**不受 mist「頁面只能帶 class」的鐵則約束**，
也不跑 `check_usage.py`——deck 是影片素材，不是產品頁面。

### 定位改成白板定位（2026-09-05）

**這是本專案目前最大的一次改動，記在最前面。**

原定位是「零 routine 的稽核系統」，服務一個方向感清楚、只缺帳的人。那是從專案發起人自己的處境推導出來的，代表性有問題——**白板上最先被寫下來的痛點恰恰是「我要怎麼訂人生目標？我要怎麼知道目標適合我？」，也就是舊定位假設不存在的那一格。**

改成白板紅便利貼那句：像健身教練幫你追蹤健身成果、安排每週菜單一樣，有人去追蹤你的人生願景跟目標。展開成五段：訂目標 → 排行動 → 排菜單 → 追執行 → 驗成效。

**四項連帶改動**

1. **零 routine 從硬約束降為預設值。** 會新增 routine 的功能一律可選，並要說明它換到什麼。設計原則第 1 條跟著改寫：判準從「有沒有新增維持成本」變成「那個成本有沒有被明確標價」
2. **每日菜單、日曆通知、社群問責從「明確不做」改成可選執行層。** 「任何形式的每日 routine」不再出現在不做清單上
3. **執行力機制從一種擴成四級**（裝錨／通知／問責獎懲／鏈上質押），預設仍是裝錨——提醒每次都要重新消耗一次意志力，錨點只消耗一次
4. **P0 從四項改五項**，補進站 1（方向假設 intake）。它一直存在於 demo 裡，只是分層表上沒有

**內層不換。** 判準層（對帳、五判準、出席率 ≠ 進展、成長式修正 vs 逃避式修改）一字未動，所以三站 demo 與差異化論述都不作廢。

**殘餘衝突（本輪未修）**

- `docs/02-差異化論述.md` 仍以「不是工作鏈，是稽核系統」開場，且沒有回應「跟健身教練類 app 有什麼不同」這條新戰線。**待處理。**
- 三站 demo 的 HTML 完全未動。頁面上沒有每日層、沒有執行力機制的第 2–4 級
- 白板的「跨使用者課表成效交叉分析」與 `docs/02` 的護城河論述（「它不會為一個人做」）方向相反。列在 P2 並標明它會改變產品的資料前提，不在本案內解決

### 痛點補第 14 項，一句話目標改寫（2026-09-05）

依 CLAUDE.md 的要求記錄與原規格的實質出入。

原規格的一句話目標綁死在「方向感清楚、但不做紀錄、自主項目容易拖」的單一使用者上。新增 **D 組 · 方向缺口**（第 14 項：訂不出目標，或不確定目標適不適合自己）之後，那句話與痛點清單自相矛盾，所以一併改寫成涵蓋兩種使用者。

第 14 項的承接機制不是新做的——站 1 的 role model、系統生成的形狀建議、反差比對本來就在回答它（見下方 2026-09-05 的三則站 1 決策）。**改的是承認它是一項缺口**，而不是把它當成站 1 的實作細節。

### 三站全部改用 mist 設計系統（2026-09-06）

原本三頁是自成一套的單檔（petrol 綠 + Noto Serif TC + IBM Plex Mono 的「會計報表」視覺）。
改成用 `ui-kit/` 裡的 mist 設計系統重做，來源在 `ui-kit/pages/`，單檔是產生出來的。

**為什麼改**：頁面各寫各的樣式，改一頁就要手動同步另外兩頁；而且那套視覺沒有元件庫可以往下接。
換成 mist 之後頁面只放 class，`./check.sh` 會擋掉寫死的顏色尺寸與 inline style。

**三條與原規格衝突的地方，記在這裡**：

1. **沒有深色模式。** mist 的亮度分層要整條反轉才能做深色，目前庫裡沒做，所以三頁只有亮色。
   這違反原本 `CLAUDE.md` 的「亮暗兩套都要能看」。
2. **沒有等寬字與襯線字。** mist 只有 Plus Jakarta Sans + Noto Sans TC。「帳簿紙 + 等寬科目代號」
   的視覺載體消失了——會計語彙只剩用詞，不再有字體上的對應。
3. **站 1 的堆疊長條圖改成 lollipop。** mist 明文禁止實心色塊圖表（「圖表用線不用面」）。
   同一組數字（62/16/14/5/3）改用五欄 lollipop 表達，預設選中「未分類」那一欄。

判準層的內容一字未動——診斷五判準、錨點缺口、出席率 ≠ 進展、成長式修正 vs 逃避式修改都還在。

**為此在設計系統裡新增了五個元件**（在庫裡做，不在專案裡做）：

| 元件 | 為了哪一頁 |
|---|---|
| SpecCard | 站 1 的六個 role model 樣板（並列比較，每張帶固定欄位） |
| Disclosure | 站 2 的五個分支手風琴 |
| Slider | 站 2 的路徑吸引力評分 |
| Segmented | 站 3 的空檔調度三組輸入、週檢查的對／不對 |
| Table | 站 3 的對帳表（群組列、數字欄、狀態色條、橫向捲） |

另外修掉三個只有實際算繪才看得到的問題：`.mist-stats` 在窄寬度會互相蓋住、lollipop 欄數塞不下時
最後一欄被切掉、SpecCard 的 grid 在容器比欄寬下限窄時整片溢出。

### 原型改用 GitHub Pages 發佈（2026-09-06）

三站現在同時存在於兩個通路：

| 通路 | 網址 | 特性 |
|---|---|---|
| GitHub Pages | https://wu0h9625-boop.github.io/guru-intake-prototype/ | 公開、可直接貼給別人、三站之間是站內相對連結 |
| Claude Artifact | 見 CLAUDE.md 的表 | 私有（要自己開分享）、三站之間是絕對網址 |

`ui-kit/scripts/inline_page.py` 從同一份來源產生兩種形態：加 `--standalone` 是完整 HTML 文件（給 Pages），
不加是 Artifact 片段，且會把站與站之間的相對連結換成 Artifact 網址。

**站 1 的 Artifact 目前是衝突狀態**——2026-09-06 有另一個 session 或頁面自存重新發佈過它，
本地副本比線上舊。要再發佈前必須先讀回來比對。

### 三站的順序是後來改的

原本只做了對帳頁。後來確認 demo 應該從匯入資料與問答開始、AI 生成計畫給使用者確認、之後才是對帳——所以補了站 1 與站 2。**順序本身是產品主張**：計畫是被確認的，不是被規定的。

### 站 1 的假想使用者刻意不是專案發起人

發起人方向感清楚、只缺帳。但多數使用者答不出「你的願景是什麼」。所以站 1 的假想使用者是**沒有方向的設計師**（5 年 3 份工作、同一領域、零 side project、近 3 個月 3 次獵頭面談）。

### role model 樣板不是勵志貼圖，是對照基準

樣板存在的理由是「讓『我不知道』變成一個可以往下走的答案」。但**選了樣板不等於有願景**，所以：

1. 每個樣板都標**代價**——沒有代價的樣板，所有人都選最亮的那個
2. 選完之後系統拿行事曆與履歷**回頭對照**，指出落差
3. 輸出明確標成**假設 v0**，不是願景

第 2 點是站 1 的核心：它把整個系統的反差機制提前到第一分鐘出場。

**2026-09-05 部分修正**：三條原則全部保留（標代價、回頭對照、輸出標假設 v0），但「六張卡片供選」的形式被推翻——見下方兩則。樣板現在是系統生成的建議，且對照基準的主體從生活形狀換成可重測能力。

### 「有做但沒效」是後來補的第 13 項痛點

行動與效果的因果分兩層：

- **第一層 行動 → 指標**（shadowing 有讓口說變好嗎）→ 累積型的能力重測就是答案，MVP 做得到
- **第二層 指標 → 上層目標**（口說變好真的通往那個職涯嗎）→ 需要對照組與數年，**軟體做不到，誠實放掉**

MVP 的處理是在對帳的三種結果之外加**第四種：有行動、但重測沒動**。這是儀表板多一格，不是新子系統。第二層改成外顯化的賭注：每個分支加「效果假設」與「反證條件」兩個欄位，讓假設變成可反證，而不需要變成可證明。

站 2 的英文口說分支簽下反證條件 → 站 3 該條件達成 1/2。**同一個欄位在兩站之間走完一輪**，這是這個論點在畫面上的完整證據。

### 視覺方向選了會計報表，不是習慣追蹤 app

規格文件的語彙本身就是會計的——對帳、歸戶、帳、稽核。所以做成帳簿紙的冷灰綠、細線分隔、等寬數字、左欄科目代號。刻意避開習慣追蹤 app 的樣子。

**2026-09-05 定位改動後仍然成立**，但理由要換一個說法：不再是「那類產品是被排除的對象」（每日菜單與通知現在是可選層），而是**判準層才是差異化所在，主敘事的視覺要長得像判準層**。可選執行層之後若做出畫面，可以是另一種語彙。

### 三頁的形式刻意不同

站 1 選擇頁用卡片、站 2 計畫書用縮排大綱、站 3 報表用表格。共用色與字讓它們是同一個產品，不同形式讓它們是不同畫面。

---

### 站 1 的期間限縮為一年（2026-09-05）

規格的目標樹是五層（願景／5 年／年度／12 週／每週），intake 原本的維度是人生階段。**這一版把站 1 的產出限縮成一年期假設。**

理由是 intake 產不出願景層與五年層——它只能借。借一年的東西比借五年的東西誠實，而且一年剛好夠讓累積型能力被重測 2–4 次（**一次重測沒有趨勢**），又短到反證條件會在使用者還在乎的時候到期。

這與 `docs/01-solution.md` 的目標樹層級有出入，依 CLAUDE.md 的要求記錄在此。不是否定那兩層，是承認 intake 這一站構不到。代價寫在 `docs/03-站1-intake-結構.md` 的 C1：站 2 的五年層會失去上游來源。

### role model 從「生活形狀」改成「像誰的什麼能力」

白板提的輸入是「如 Stephen Curry 一般的三分球能力」——具體人物＋具體能力。原本站 1 是六個抽象生活形狀。

改成能力為主的理由不是它比較具體，是**可指名的能力天生自帶重測方式**。「深耕的專家」沒有命中率，「三分命中率」有。而重測方式正是本文件「未決事項」裡被升為阻擋項的那一條——沒有重測方式的累積型分支，在站 3 儀表板上會永遠停在「效果未知」。

讓使用者在站 1 自己講出可重測的能力，等於讓他把重測方式帶進來。這條線接到站 2 的累積型分支欄位、站 3 的「出席 ✓ ｜ 效果 ✗」。形狀降為輔助，但保留「代價」欄位與反差比對。

### 六個形狀從「選單」改成「系統生成的建議」

原本是擺好六張卡片供選。改成系統依 (匯入資料 ＋ 基本題 ＋ role model) 生出 2–3 張建議，**每張必帶依據行**。

擺好的選單是我們的分類法，不是使用者的處境；而且六選一會讓「我不知道」變成「我隨便挑一個」——那比空白更糟，因為它看起來像答案。依據行的作用是讓卡片可以被反駁：那一行寫的事情使用者不認同，卡片就不成立。

這推翻了 CLAUDE.md 裡「站 1 是選擇頁 → 可選的樣板卡片陣列」那條規則（見 C4）。

### 匯入來源改成白板四項，不放信用卡帳單

calendar P0 / Notion P1 / 履歷 P2 / Apple 健康 P3。Apple 健康補上四要素裡「能量需求」的來源，Notion 補「隱形投入」——這兩項是現行頁面沒有的。

代價是**金錢流沒有來源**（C2）：站 3 空檔調度的三軸之一、以及對帳表的購課那筆，目前是懸空的。這是明知的取捨，不是疏漏。

---

## 未決事項

本專案的未決事項**只維護在這裡一份**。`docs/01-solution.md` 於 2026-09-05 重整時把它的第十六節整段移了過來，該檔不再保留平行清單。

### 承自規格文件（`docs/01-solution.md`）

- 年度層要幾個分支、怎麼切（三大領域只是起點，未收斂）
- 歸戶規則——模糊與跨分支的行動怎麼判定
- 累積型第二個常設名額放什麼（語言算一個，另一個未定）
- 各累積型分支的重測方式與基準線 ← **已升為阻擋項**，因為重測是唯一的效果偵測器；沒有重測方式的累積型分支在儀表板上會永遠停在「效果未知」
- 第一季樂觀係數的冷啟動預設值（頁面上暫用 ×0.6）
- 空檔調度的判斷邏輯要多明確——金錢流門檻（頁面上暫用結餘正負）與時間視野怎麼量化
- 「預設懷疑目標」要不要改成對稱版本
- 觸發式插入用什麼管道送達
- 實作方式三案（全借現成／全自建／混合）未選定
- **進展的呈現形式**（自 01-solution 移入）
- 資料存放位置與隱私邊界

### 白板帶進來的（自 01-solution 第十六節移入，2026-09-05）

- **心理學特性題與選項排序**（Steven）要不要進站 1、放第幾段。它跟基本題的差別是它問偏好不問事實，而偏好題有作答成本
- **身體狀況分數怎麼換算成能量需求**。Garmin 身體電量與「這個動作做不做得動」之間目前沒有對照表
- **每日層的開啟時機**。預設關閉，但什麼時候該建議使用者打開，判準未定
- **執行力機制升級的判準**。第 1 級無效多久之後才建議升到第 2 級
- **偉人／人生導師的行動模板庫**從哪來，以及它跟 role model 的關係
- **願景怎麼寫得好**。願景是整棵樹唯一不可從痕跡推導的層，方法論值得獨立展開：要足夠具體才推導得下去、足夠穩定才能當多年基準、但允許逐年微調、且不能是自我美化的版本

### 站 1 改版帶出的跨站衝突（詳見 `docs/03-站1-intake-結構.md` 第五節）

- **C1 站 2 的五年層失去上游**。站 1 產出一年期假設，站 2 卻從五年路徑展開，中間斷一層。站 2 要改成 一年 → 4 季 → 12 週 → 每週，還是保留五年層並標明「本期未涵蓋」
- **C2 金錢流沒有來源**。拿掉帳單後，站 3 的金錢流軸與購課那筆懸空。三選一：站 3 改兩軸／金錢流改由 Notion 或手動輸入／帳單以 P4 選填補回站 1
- ~~**C3 設計系統分裂**~~ **已解決（2026-09-06）**。站 2、站 3 也換成 mist，三站一致；CLAUDE.md 的「設計系統」章節已重寫
- **C4 CLAUDE.md 的站 1 定位過時**。「選擇頁 → 樣板卡片陣列」要改成「輸入 → 系統生成 → 反駁或接受」

### 站 1 新結構本身未決的

- 能力拆解規則（「像誰的什麼能力」→ 可重測能力／隱含形狀／代價），以及抽象能力拆不出重測方式時的處理流程
- 形狀建議的生成規則：依據行三種來源怎麼加權、幾張卡片、推不出來時顯示什麼
- 未匯入資料時那份處方的實際內容
- **可重測能力的基準線首測怎麼引導**。三分球要去球場投 100 球——這件事的引導完全還沒想，而沒有基準線就沒有重測

### 本次新增

- **role model 樣板要預設哪幾種**。目前六個是示範用（深耕的專家／從零到一的建造者／獨立經營者／帶人的人／穩定的支柱／跨界的連結者），沒有經過驗證
- **樣板與目標樹的映射規則**。選了樣板之後，五年路徑與累積型能力要怎麼自動生成，目前是硬編
- **反差比對的判準要多明確**。現在每個樣板的落差項是手寫的，真做要有規則
- 三站之間的狀態怎麼傳（原型是三個獨立頁面，沒有共用狀態）

---

## 下一步

三天要動手的話，順序是：

1. **目標樹資料模型**——四要素、型別、效果假設與反證條件欄位。這是排程器與對帳的積木，必須先於一切
2. **對帳引擎**——痕跡進、四種結果出（有行動／零行動／歸不進去／有行動但無效果）
3. **診斷儀表板**——四判準
4. **空檔調度問答**——最容易 demo 出 wow，優先度不低於對帳

站 1 與站 2 的頁面已經把資料模型的形狀畫出來了，可以直接照著頁面上的欄位建 schema。

在那之前有一件排在最前面的事：**依 `docs/03-站1-intake-結構.md` 改寫 `demo/01-intake.html`**。結構已收斂（七段：期間／匯入／基本題／role model／系統建議的形狀／反差／產出），HTML 尚未動。改的時候直接改原檔，並用 Artifact 帶 `url` 參數重新發佈，否則三頁硬編連結會斷。

---

## 已知的原型限制

- 三頁都是前端假資料，沒有後端、沒有資料庫、沒有 API 串接
- 站 1 的檔案上傳是靜態畫面，不會真的解析 PDF 或行事曆
- 站 2 按下「接受並鎖定」只改變前端狀態，不會存任何東西
- 站 3 的排程重算只算頁面上那五列，不是真的排程器
- 沒有人在瀏覽器裡逐頁目視驗證過渲染結果（發佈當時無法截圖），若有破版需要人工回報
